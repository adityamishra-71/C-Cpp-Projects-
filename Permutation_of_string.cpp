#include<iostream>
using namespace std;

void generate_Permutation(text, text.length) {
    
}

int main() {
string text;
cout << "Input a string: ";
cin >> text;

cout << "All permutations of the string: " << endl;
generate_Permutations(text, 0, text.length() - 1);

return 0;
}